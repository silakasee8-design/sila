# Weather dashboard

SILA AI includes a standalone weather dashboard at `/weather-dashboard.html`.

It uses the public Open-Meteo geocoding and forecast APIs, so no weather API key is required. Search for a city to see current temperature, condition, wind, humidity, feels-like temperature, sunrise, and a seven-day forecast.

Open locally at:

```text
http://localhost:3000/weather-dashboard.html
```
